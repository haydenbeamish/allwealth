# Laserbeam Capital API Key - required for all /api/* requests
# Get this from your Laserbeam Capital admin
VITE_API_KEY=your_api_key_here

# API Base URL - only needed for production builds (GitHub Pages).
# In local dev, the Vite proxy handles /api routing automatically.
# VITE_API_URL=https://api.laserbeamcapital.com/api
